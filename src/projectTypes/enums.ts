export enum HTTPMethod {
  GET = 'GET',
  POST = 'POST',
}
export enum Meta {
  initial = 'initial',
  loading = 'loading',
  error = 'error',
  success = 'success',
}
export enum LoaderSize {
  s = 's',
  m = 'm',
  l = 'l',
}

export enum ButtonColor {
  primary = 'primary',
  secondary = 'secondary',
}
